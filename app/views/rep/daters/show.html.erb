<% provide(:title, @event.title) %>
<%= render 'rep/events/events_links' %>
<%= render 'rep/events/event_links' %>

<div class="row">
  <aside class="col-md-4">
    <section>
      <h1><%= @event.title %></h1>

      <%= render 'shared/match_list', path: rep_event_dater_path %>
    </section>
  </aside>
</div>