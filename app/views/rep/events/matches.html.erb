<% provide(:title, @event.title) %>
<%= render 'rep/events/events_links' %>
<%= render 'rep/events/event_links' %>

<div class="row">
  <aside class="col-md-4">
    <section>
      <h1><%= @event.title %></h1>

      <h2>Matches</h2>

      <%= render 'shared/match_table' %>

    </section>
  </aside>
</div>