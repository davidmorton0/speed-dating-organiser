<tr>
  <td><%= link_to dater.id, admin_event_dater_path(@event, dater) %></td>
  <td><%= link_to dater.name, admin_event_dater_path(@event, dater) %></td>
  <td><%= dater.email %></td>
  <td><%= dater.phone_number %></td>
  <td><%= dater.gender %></td>
  <td><%= link_to 'Delete', "/admin/events/#{@event.id}/daters/#{dater.id}", data: { "turbo-method": :delete, turbo_confirm: 'Delete Dater. Are you sure?' }, class: "btn btn-danger ml-3" %></td>
</tr>